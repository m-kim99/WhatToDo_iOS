name: WhatToDo
options:
  bundleIdPrefix: com.legendherokim
  deploymentTarget:
    iOS: "17.0"
  defaultConfig: Debug

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  WhatToDo:
    type: application
    platform: iOS
    sources:
      - WhatToDo
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.legendherokim.whattodo
        INFOPLIST_FILE: WhatToDo/Info.plist
    scheme:
      testTargets: []
